#include <iostream>
#include<cmath>
using namespace std;
int main()
{
float x,y,a;
cout<<"ingrese x";
cin>>"x";
a=(x+(x*x))/(5*x)+3);
y=(a+x)*((a+(2*x)));
cout<<"el valor de y es:";
cout<<y;
}
return 0;
}
